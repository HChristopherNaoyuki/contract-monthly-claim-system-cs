@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<div class="error-container">
    <h1 class="error-title">Error</h1>
    <h2 class="error-message">An error occurred while processing your request.</h2>

    @if (Model.ShowRequestId)
    {
        <div class="error-details">
            <strong>Request ID:</strong> <code>@Model.RequestId</code>
        </div>
    }

    <div class="error-info">
        <h3>Development Mode</h3>
        <p>
            Swapping to <strong>Development</strong> environment will display more detailed information about the error.
        </p>
        <p>
            <strong>Don't enable Development mode for deployed applications.</strong>
            It may display sensitive information to end users.
        </p>
    </div>
</div>